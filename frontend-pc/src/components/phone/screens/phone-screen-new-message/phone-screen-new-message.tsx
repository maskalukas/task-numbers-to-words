import { PhoneKeyboard } from "../../hoc/phone-keyboard/phone-keyboard";

export const screenRoute = "new-message";

export const PhoneScreenNewMessage = () => {
    return (
        <div>
            <PhoneKeyboard></PhoneKeyboard>
        </div>
    )
}

